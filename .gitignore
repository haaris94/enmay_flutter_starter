# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
#ignore generated files
**/*.g.dart
**/*.freezed.dart
**/*.g.freezed.dart
**/*.mapper.dart

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Keep dependency lock files for consistent builds across machines
# pubspec.lock - Keep committed for consistent dependency versions across machines
# ios/Podfile.lock - Keep committed for consistent iOS builds

# Platform-specific build caches that should be ignored
**/ios/Flutter/.last_build_id
**/android/.gradle/
**/android/app/build/
**/build/

# IDE and editor specific files (optional - comment out if you want to share)
.vscode/settings.json
.vscode/launch.json
